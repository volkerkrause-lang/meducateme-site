(()=>{const hero=document.getElementById('hero');const stages=[...document.querySelectorAll('.stage')];const bar=document.getElementById('bar');const progress=document.getElementById('progress');let i=-1;function show(n){i=n;hero.style.display=n<0?'grid':'none';stages.forEach((s,j)=>s.classList.toggle('active',j===n));progress.textContent=n<0?'INTRO':`${String(n+1).padStart(2,'0')} / ${String(stages.length).padStart(2,'0')}`;bar.style.width=n<0?'7%':`${((n+1)/stages.length)*100}%`;window.scrollTo({top:68,behavior:'smooth'})}document.getElementById('start').onclick=()=>show(0);document.addEventListener('click',e=>{if(e.target.closest('[data-next]'))show(Math.min(stages.length-1,i+1));if(e.target.closest('[data-prev]'))show(Math.max(0,i-1));const c=e.target.closest('.choice');if(c){const box=c.closest('.quiz');box.querySelectorAll('.choice').forEach(x=>x.classList.remove('good','bad'));c.classList.add(c.dataset.answer==='good'?'good':'bad');box.querySelector('.feedback').textContent=c.dataset.answer==='good'?'Exactly. Insulin is present, but the tissues are not responding normally to the signal.':'Not quite. Follow the pathway from insulin concentration to tissue response.'}});const v=document.getElementById('insulinVideo');const missing=document.getElementById('videoMissing');const bg=document.getElementById('bgGauge');const ins=document.getElementById('insGauge');const chs=[...document.querySelectorAll('.chapter')];v.addEventListener('error',()=>missing.style.display='grid');v.addEventListener('loadedmetadata',()=>missing.style.display='none');chs.forEach(b=>b.onclick=()=>{v.currentTime=+b.dataset.t;v.play().catch(()=>{})});v.addEventListener('timeupdate',()=>{const t=v.currentTime;const idx=t<22?0:t<48?1:t<78?2:3;chs.forEach((b,j)=>b.classList.toggle('active',j===idx));let bgv,iv;if(t<22){bgv=5+(t/22)*3.2;iv='LOW'}else if(t<48){bgv=8.2-(t-22)/26*.5;iv='RISING'}else if(t<78){bgv=7.7-(t-48)/30*1.9;iv='HIGH'}else{bgv=5.8-(t-78)/12*.8;iv='FALLING'}bg.textContent=bgv.toFixed(1);ins.textContent=iv});show(-1)})();